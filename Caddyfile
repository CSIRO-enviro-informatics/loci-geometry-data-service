# Combination, check for HTTP scheme and redirect
http://*.loci.cat {
  redir https://{host}{uri}
  cors
}
https://*.loci.cat {
  tls /etc/ssl/wildcard-loci-cat.bundle.pem /etc/ssl/wildcard-loci-cat.pem
  proxy / http://gservice:3000 {
     transparent
  }
  cors
}
